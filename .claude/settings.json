{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "PROJECT_DIR=$(git rev-parse --show-toplevel 2>/dev/null || pwd); if [ -f Package.swift ]; then BUILD_OUT=$(swift build 2>&1); EXIT=$?; if [ $EXIT -ne 0 ]; then echo 'BUILD FAILED — fix before continuing:'; echo \"$BUILD_OUT\" | grep -E '(error:|warning:)' | head -10; exit 1; else echo 'Build OK'; fi; elif XCPROJ=$(ls -d *.xcodeproj 2>/dev/null | head -1); [ -n \"$XCPROJ\" ]; then SCHEME=\"${XCPROJ%.xcodeproj}\"; SIM_DEST=$(bash \"$PROJECT_DIR/scripts/find-simulator.sh\" 2>/dev/null); [ -z \"$SIM_DEST\" ] && SIM_DEST='platform=iOS Simulator,name=iPhone 16'; BUILD_OUT=$(xcodebuild build -project \"$XCPROJ\" -scheme \"$SCHEME\" -sdk iphonesimulator -destination \"$SIM_DEST\" -quiet 2>&1); EXIT=$?; if [ $EXIT -ne 0 ]; then echo 'BUILD FAILED — fix before continuing:'; echo \"$BUILD_OUT\" | grep -E '(error:|warning:)' | head -10; exit 1; else echo 'Build OK'; fi; fi",
            "description": "Build after edits — auto-detects SPM or Xcode project and simulator"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash scripts/stop-gate.sh",
            "description": "Block stop and re-inject next task until all features pass"
          }
        ]
      }
    ]
  }
}
